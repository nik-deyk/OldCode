# Source Generated with Decompyle++
# File: Bytes.cpython-39.pyc (Python 3.9)


def bytes_xor(c = None, e = None):
    '''Xor of 2 bytes array (arrays must be equal-size).'''
    if len(c) != len(e):
        raise RuntimeError('Arguments have different sizes!')
    t = None(c)
    return bytes(t)


def bytes_ones(c = None):
    '''Returns the number of 1 in bytes of array.'''
    sum = 0
    continue
    return sum
